////////////////////
// Note that this might not work for large size database
//
MATCH (a)-[r]->() DELETE a, r;

////////////////////
// Note that this might not work for large size database
//
MATCH (a) DELETE a;
